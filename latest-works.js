document.querySelectorAll('.lw-btn').forEach(btn => {
    btn.addEventListener('click', function(e) {
        // Optional: Add a click animation or analytics here
        // e.g., btn.classList.add('clicked');
    });
});